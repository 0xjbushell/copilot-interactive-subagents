{"schema": "tix/v1", "id": "TIX-000001", "type": "Story", "description": "Story: Copilot interactive subagents for Copilot CLI", "context": "Outcome: Any Copilot CLI user can launch, monitor, and resume built-in or custom agents in visible tmux/cmux/zellij panes, using the pi extension as the behavioral reference model.\n\nSpec: /home/jb/.projects/copilot-interactive-subagents/specs/subagents/copilot-interactive-subagents.md\nShared Key: copilot-interactive-subagents\n\nWave structure target:\n- Wave 1: Slice 0\n- Wave 2: Slice 1\n- Wave 3: Slice 2 + Slice 3\n- Wave 4: Slice 4", "assignee": null, "tags": ["copilot-interactive-subagents", "subagents"], "agentState": "Done", "ado": null, "parent": null, "blockedBy": [], "subtasks": [], "completion": {"result": "Implemented the generic Copilot CLI interactive-subagents extension with pane-backed orchestration, resume, operator-facing tools, and validated test/coverage/CRAP/mutation gates."}, "close": {"status": "pending_close", "pr": null, "closedAt": null}}
{"schema": "tix/v1", "id": "TIX-000002", "type": "Feature", "description": "Core pane-backed subagent orchestration", "context": "This feature delivers the core pane-backed orchestration behavior: backend detection/attach, single-agent launches, parallel launches, completion monitoring, summaries, and resume.\n\nSpec Context\n- File: /home/jb/.projects/copilot-interactive-subagents/specs/subagents/copilot-interactive-subagents.md\n- Lines: 15-26 (## Scope / In Scope), 36-48 (## Design Decisions), 81-129 (## Architecture / Component Overview / Data / Control Flow), 214-256 (## Success Criteria / ## Failure Modes / ## Testing Strategy)\n- Adjacent: /home/jb/.projects/copilot-interactive-subagents/specs/README.md, /home/jb/.projects/copilot-interactive-subagents/specs/SPEC_TEMPLATE.md", "assignee": null, "tags": ["copilot-interactive-subagents", "subagents"], "agentState": "Done", "ado": null, "parent": "TIX-000001", "blockedBy": [], "subtasks": [], "completion": {"result": "Accepted Slice 0 and Slice 1 with fresh repo-level verification of exact-name agent discovery, mux prerequisites, single-pane launch, state persistence, and quality gates."}, "close": {"status": "pending_close", "pr": null, "closedAt": null}}
{"schema": "tix/v1", "id": "TIX-000003", "type": "Feature", "description": "Reusable workflow integration and operator guidance", "context": "This feature makes the orchestration reusable: the extension must expose generic tools callable by Copilot and be documented well enough for future Agent Skills and operators to use safely.\n\nSpec Context\n- File: /home/jb/.projects/copilot-interactive-subagents/specs/subagents/copilot-interactive-subagents.md\n- Lines: 24-25 (tool interfaces / generic launch tool), 45-47 (generic launch tool and skill relationship), 97-115 (skill-facing orchestration interface), 180-202 (key interfaces), 204-256 (changes, success criteria, risks, testing)\n- Adjacent: /home/jb/.projects/copilot-interactive-subagents/specs/README.md", "assignee": null, "tags": ["copilot-interactive-subagents", "subagents"], "agentState": "Done", "ado": null, "parent": "TIX-000001", "blockedBy": [], "subtasks": [], "completion": {"result": "Accepted Slices 2-4 with fresh repo-level verification of parallel orchestration, resume, namespaced public tools, title helper, documentation, and quality gates."}, "close": {"status": "pending_close", "pr": null, "closedAt": null}}
{"schema": "tix/v1", "id": "TIX-000004", "type": "Deliverable", "description": "Slice 0: backend discovery and launch prerequisites", "context": "## Acceptance Criteria\n- GIVEN the extension runs in a project with supported multiplexers available WHEN the discovery/listing path is invoked THEN it returns the exact runtime-recognized agent identifiers and the supported multiplexer backends available for launch.\n- GIVEN the user is not already inside a supported multiplexer WHEN automatic start or attach is reliable for the detected backend THEN the extension selects that backend and continues without requiring manual pane setup.\n- GIVEN no supported backend can be attached or started WHEN a launch is requested THEN the extension returns setup guidance and does not leave orphaned launch metadata behind.\n\n## Spec Context\n- File: /home/jb/.projects/copilot-interactive-subagents/specs/subagents/copilot-interactive-subagents.md\n- Sections: ## Scope, ## Design Decisions, ## Architecture, ### Backend Selection and Attach/Start Policy, ### Agent Target Resolution Policy, ### Key Interfaces, ## Success Criteria, ## Failure Modes / Risks, ## Testing Strategy\n- Subsystem: subagents \u2014 multiplexer-backed Copilot subagent orchestration and session handoff\n- Adjacent: /home/jb/.projects/copilot-interactive-subagents/specs/README.md, /home/jb/.projects/copilot-interactive-subagents/specs/SPEC_TEMPLATE.md\n- Note: section references are canonical; do not rely on line numbers in a living spec.\n\n\n## Traceability\n- Shared Key: copilot-interactive-subagents\n- Requirement Refs: None\n- Source: explicit spec ## Traceability\n\n## Must-Haves\nTruths\n- Users can see which supported backends and agent identifiers are actually launchable.\n- Users get exact-name validation before a pane launch starts.\n- Users get reliable setup guidance when automatic attach/start is not possible.\n\nArtifacts\n- `.github/extensions/copilot-interactive-subagents/extension.mjs` \u2014 extension entrypoint with discovery/listing hooks\n- `.github/extensions/copilot-interactive-subagents/lib/mux.mjs` \u2014 backend detection and attach/start logic\n- `.github/extensions/copilot-interactive-subagents/lib/agents.mjs` \u2014 installed-agent discovery and exact-name validation\n- `test/mux-discovery.test.*` \u2014 backend discovery and fallback coverage\n- `test/agent-discovery.test.*` \u2014 installed agent listing / validation coverage\n\nKey Links\n- `extension.mjs` routes list/launch requests through `agents.mjs` and `mux.mjs` before any pane creation occurs.\n- `mux.mjs` and `agents.mjs` must agree on what qualifies as a launchable environment so invalid launches fail before state is written.\n\n## Implementation Notes\n- Port the backend detection ideas from the pi extension's `cmux.ts`, but keep the API generic to Copilot CLI.\n- Keep V1 targeting exact-name based; do not add alias or role resolution in this slice.\n- If automatic attach/start differs by backend, expose that difference explicitly in the result rather than hiding it.\n\n## Adversarial Test Cases\n- User requests agent `planner` but only `reviewer` exists -> validation must fail before pane creation.\n- Backend is partially detectable but attach/start command fails -> return actionable guidance and leave no stale launch record.\n- Multiple backends are present with conflicting signals -> choose deterministically and report which backend was selected.\n\n## Affected Test Files\n- No existing tests affected (new project).\n\n## Test Matrix\n\n### ATDD Scenarios\n- GIVEN supported multiplexers and installed agents exist WHEN the discovery/listing path runs THEN it reports launchable backends and exact runtime-recognized agent identifiers.\n- GIVEN the user is outside a supported multiplexer WHEN automatic attach/start is reliable THEN the extension attaches or starts successfully and records the selected backend.\n- GIVEN attach/start is not possible WHEN launch prerequisites are checked THEN the extension returns setup guidance without stale launch metadata.\n\n### Unit Tests\n- backend priority and selection rules\n- env/runtime detection for tmux/cmux/zellij\n- exact-name agent validation and normalization\n- attach/start decision logic\n- prerequisite failure result shaping\n\n### Integration Tests\n- discovery path wired through extension entrypoint + mux adapter + agent listing\n- attach/start probing per backend where automation is practical\n- invalid backend or missing agent path produces no persisted launch record\n\n### End-to-End Tests\n- load the installed extension and invoke the generic discovery/prerequisite tool from a parent Copilot session\n- verify the reported backend and agent list matches the runtime environment\n\n### Mutation Testing\n- required for deterministic logic modules in this slice\n- primary targets: backend selection, agent validation, attach/start decision logic\n- target mutation score: >= 80%\n- shell-command glue may be excluded if it remains thin and covered by integration tests\n\n### CRAP Score Evaluation\n- CRAP < 8 required for new logic-heavy functions in backend selection and prerequisite decision code\n- CRAP > 30 is always unacceptable\n- infrastructure wrappers are advisory unless complexity grows into central decision logic\n\n## Backend and Agent Discovery Contract\n\nBackend decision policy for this slice:\n1. If the request explicitly names a backend, use it or fail with a backend-specific error.\n2. Otherwise, attach to an already active supported backend when detected via runtime environment.\n3. Otherwise, start a new backend session only when that backend is installed and startup is explicitly supported.\n4. Otherwise, fail with setup guidance and do not create launch metadata that looks resumable.\n\nAgent resolution policy for this slice:\n- Custom agents: use documented Copilot-supported discovery mechanisms where available.\n- Built-in agents: accept explicit identifiers even if the runtime cannot enumerate them in advance.\n- V1 rejects aliases, fuzzy matches, and role inference.\n- Validation failures return structured errors with a stable code, the requested identifier, and any known available identifiers.\n\nStable failure codes for this slice:\n- `BACKEND_UNAVAILABLE`\n- `BACKEND_START_UNSUPPORTED`\n- `AGENT_NOT_FOUND`\n- `AGENT_VALIDATION_UNAVAILABLE`\n", "assignee": null, "tags": ["copilot-interactive-subagents", "subagents"], "agentState": "Done", "ado": null, "parent": "TIX-000002", "blockedBy": [], "subtasks": [], "completion": {"result": "Accepted after independent verification: npm test pass, npm run test:coverage pass, npm run test:crap pass (all 20 functions below CRAP 8), npm run test:mutation pass (11/11 killed)."}, "close": {"status": "pending_close", "pr": null, "closedAt": null}}
{"schema": "tix/v1", "id": "TIX-000005", "type": "Deliverable", "description": "Slice 1: single pane-backed subagent launch and summary", "context": "## Acceptance Criteria\n- GIVEN a valid backend and exact agent identifier WHEN the generic single-launch tool is invoked THEN the extension opens a visible pane, starts the target Copilot agent in that pane, and returns pane/session metadata to the parent session.\n- GIVEN the child subagent completes successfully WHEN monitoring detects completion THEN the parent session receives the final summary, exit status, and a resume pointer.\n- GIVEN launch metadata was written before completion WHEN the parent session exits and returns later THEN the stored launch record remains readable for follow-up or resume handling.\n\n## Spec Context\n- File: /home/jb/.projects/copilot-interactive-subagents/specs/subagents/copilot-interactive-subagents.md\n- Sections: ## Scope, ## Design Decisions, ## Architecture, ### Session State and Resume Model, ### Completion Detection and Summary Contract, ### Key Interfaces, ## Success Criteria, ## Failure Modes / Risks, ## Testing Strategy\n- Subsystem: subagents \u2014 single pane-backed launch, summary, and stored launch metadata\n- Adjacent: /home/jb/.projects/copilot-interactive-subagents/specs/README.md\n- Note: section references are canonical; do not rely on line numbers in a living spec.\n\n\n## Traceability\n- Shared Key: copilot-interactive-subagents\n- Requirement Refs: None\n- Source: explicit spec ## Traceability\n\n## Must-Haves\nTruths\n- Users can launch one selected agent into a visible pane.\n- Users receive a structured summary back in the parent session when the child finishes.\n- Users can inspect or reuse launch metadata after the first run.\n\nArtifacts\n- `.github/extensions/copilot-interactive-subagents/lib/launch.mjs` \u2014 single-launch orchestration\n- `.github/extensions/copilot-interactive-subagents/lib/state.mjs` \u2014 launch manifest and summary persistence\n- `.github/extensions/copilot-interactive-subagents/lib/summary.mjs` \u2014 completion detection and summary extraction\n- `test/single-launch.test.*` \u2014 single-launch lifecycle coverage\n- `test/state-store.test.*` \u2014 launch metadata persistence coverage\n\nKey Links\n- `launch.mjs` must call `state.mjs` before and after child execution so resume metadata survives interruptions.\n- `summary.mjs` must interpret the child session output in a way that `launch.mjs` can map back to the parent result shape.\n\n## Implementation Notes\n- Keep the generic launch tool stable enough that future Agent Skills can call it directly.\n- Prefer Copilot-native session identifiers and workspace-backed storage over inventing a parallel session format.\n- Mirror the pi extension's notion of a final branch summary, but adapt it to Copilot session semantics.\n\n## Adversarial Test Cases\n- Pane opens but child Copilot launch fails immediately -> result must include failure state and preserve enough metadata for inspection.\n- Child completes without a clear summary block -> fallback summary must still be deterministic and non-empty.\n- Parent process dies after launch metadata is written but before completion -> next session can still inspect the unfinished launch record.\n\n## Affected Test Files\n- No existing tests affected (new project).\n\n## Test Matrix\n\n### ATDD Scenarios\n- GIVEN a valid backend and exact agent identifier WHEN the single-launch tool runs THEN a visible pane opens, the child session starts, and a structured summary is returned.\n- GIVEN the child launch fails early WHEN monitoring completes THEN the parent receives a failure result with preserved launch metadata.\n- GIVEN metadata was written before completion WHEN the parent session exits and returns later THEN the unfinished launch can still be inspected or resumed.\n\n### Unit Tests\n- launch request validation and normalization\n- launch-manifest serialization\n- summary extraction and fallback summary rules\n- exit-state/result mapping\n- resume pointer construction\n\n### Integration Tests\n- single launch flow across entrypoint, mux adapter, launch service, state store, and summary service\n- pane open + child start + metadata write ordering\n- failed child start still leaves inspectable state\n\n### End-to-End Tests\n- install/load extension and launch one built-in agent into a pane\n- install/load extension and launch one custom agent into a pane\n- verify parent session receives summary, exit state, and resume reference\n\n### Mutation Testing\n- required for launch normalization, state serialization, summary extraction, and result mapping logic\n- target mutation score: >= 80%\n- shell-heavy process wrappers are mutation-advisory if integration coverage proves behavior\n\n### CRAP Score Evaluation\n- CRAP < 8 required for launch planning, summary extraction, and result-shaping functions\n- refactor any new high-complexity branching before merge\n\n## Launch Manifest Contract\n\nMinimum persisted launch fields for this slice:\n- `launchId`\n- `agentIdentifier`\n- `agentKind`\n- `backend`\n- `paneId`\n- `sessionId` (nullable if not known yet)\n- `requestedAt`\n- `status`\n- `summary` (nullable until completion)\n- `exitCode` (nullable until completion)\n- `metadataVersion`\n\nManifest-writing rule:\n- Write an initial manifest before child completion.\n- Update it on transition to `running` and again on terminal states (`success`, `failure`, `cancelled`, `timeout`).\n\n## Completion and Summary Contract\n\nCompletion detection baseline:\n- The child launch command emits an explicit exit sentinel `__SUBAGENT_DONE_<code>__`.\n- Monitoring detects that sentinel through pane output capture.\n- Backend-native hooks are optional optimizations, not the primary contract.\n\nSummary extraction order:\n1. persisted explicit summary captured from child session state\n2. last reliable assistant summary message before completion\n3. deterministic fallback summary derived from exit state and pane/session metadata\n\nThe returned summary must never be empty.\n", "assignee": null, "tags": ["copilot-interactive-subagents", "subagents"], "agentState": "Done", "ado": null, "parent": "TIX-000002", "blockedBy": ["TIX-000004"], "subtasks": [], "completion": {"result": "Accepted after independent verification: npm test pass (37/37), npm run test:coverage pass, npm run test:crap pass (all 58 functions below CRAP 8), npm run test:mutation pass (17/17 killed)."}, "close": {"status": "pending_close", "pr": null, "closedAt": null}}
{"schema": "tix/v1", "id": "TIX-000006", "type": "Deliverable", "description": "Slice 2: parallel pane orchestration with progress tracking", "context": "## Acceptance Criteria\n- GIVEN multiple valid launch requests WHEN the parallel-launch tool is invoked THEN the extension opens a pane layout that keeps all launched agents visible in supported multiplexers and returns per-agent tracking metadata.\n- GIVEN parallel agents are running WHEN the parent session requests progress THEN the extension reports per-agent progress and completion state without mixing results between agents.\n- GIVEN one parallel agent fails while others succeed WHEN the run completes THEN the aggregate result reports partial success accurately and preserves each agent's individual summary and exit state.\n\n## Spec Context\n- File: /home/jb/.projects/copilot-interactive-subagents/specs/subagents/copilot-interactive-subagents.md\n- Sections: ## Scope, ## Design Decisions, ## Architecture, ### Completion Detection and Summary Contract, ### Key Interfaces, ## Success Criteria, ## Failure Modes / Risks, ## Testing Strategy\n- Subsystem: subagents \u2014 parallel pane orchestration and progress reporting\n- Adjacent: /home/jb/.projects/copilot-interactive-subagents/specs/README.md\n- Note: section references are canonical; do not rely on line numbers in a living spec.\n\n\n## Traceability\n- Shared Key: copilot-interactive-subagents\n- Requirement Refs: None\n- Source: explicit spec ## Traceability\n\n## Must-Haves\nTruths\n- Users can run multiple independent agents in parallel panes.\n- Users can see progress for each launched agent from the parent session.\n- Users receive separate summaries/results for each agent at completion.\n\nArtifacts\n- `.github/extensions/copilot-interactive-subagents/lib/parallel.mjs` \u2014 parallel launch coordinator\n- `.github/extensions/copilot-interactive-subagents/lib/mux-layout.mjs` \u2014 pane layout strategy per backend\n- `.github/extensions/copilot-interactive-subagents/lib/progress.mjs` \u2014 per-agent progress aggregation\n- `test/parallel-launch.test.*` \u2014 parallel lifecycle and partial-failure coverage\n\nKey Links\n- `parallel.mjs` must keep launch IDs, pane IDs, and session IDs aligned so summaries cannot cross-associate.\n- `mux-layout.mjs` must provide backend-specific layout rules that `parallel.mjs` can apply consistently.\n\n## Implementation Notes\n- Parallelism is for independent tasks; document that write-heavy agents targeting the same repo may still conflict.\n- Reuse the single-launch flow where possible instead of duplicating completion/summary behavior.\n- Preserve a deterministic ordering for returned results, even if agents finish in a different order.\n\n## Adversarial Test Cases\n- Three agents launch, one exits immediately, two continue -> progress and final reporting must remain coherent.\n- Pane creation succeeds for first agent but fails for second -> partial setup must be cleaned up or reported explicitly.\n- Two agents produce similarly worded summaries -> results must still map to the right launch records.\n\n## Affected Test Files\n- No existing tests affected (new project).\n\n## Test Matrix\n\n### ATDD Scenarios\n- GIVEN multiple valid launch requests WHEN parallel launch runs THEN panes are created deterministically and each agent remains attributable.\n- GIVEN one parallel agent fails while others succeed WHEN the run completes THEN the aggregate result reports partial success accurately.\n- GIVEN progress is requested during execution WHEN multiple agents are running THEN per-agent progress and completion states remain separated.\n\n### Unit Tests\n- launch fan-out and result fan-in mapping\n- deterministic result ordering\n- per-agent progress aggregation\n- pane/session/launch identifier correlation\n- partial-failure aggregation logic\n\n### Integration Tests\n- parallel launches across mux layout strategy + single-launch orchestration reuse\n- layout behavior per supported backend where automation is practical\n- partial pane creation or partial launch failures\n\n### End-to-End Tests\n- run at least two independent pane-backed agents in parallel and observe progress from the parent session\n- verify mixed success/failure reporting in one end-to-end run\n\n### Mutation Testing\n- required for aggregation, correlation, ordering, and progress-state logic\n- target mutation score: >= 80%\n- layout shell wrappers may be excluded if verified through integration and E2E tests\n\n### CRAP Score Evaluation\n- CRAP < 8 required for progress aggregation, result correlation, and partial-failure decision functions\n- complex orchestration branches should be split rather than tolerated\n\n## Parallel Orchestration Contract\n\nDeterminism rules for this slice:\n- Results are returned in launch-request order, not completion order.\n- Progress updates are keyed by `launchId` and must not rely on summary text or pane order alone.\n- Parallel orchestration should reuse the single-launch manifest and summary contracts rather than redefining them.\n\nCompletion and timeout rules:\n- Each child uses the same sentinel-based completion contract as Slice 1.\n- A timed-out child should produce a terminal result with status `timeout`, preserve partial metadata, and not block sibling results forever.\n- Partial failure is a first-class outcome; the aggregate result must represent mixed success/failure cleanly.\n", "assignee": null, "tags": ["copilot-interactive-subagents", "subagents"], "agentState": "Done", "ado": null, "parent": "TIX-000002", "blockedBy": ["TIX-000005"], "subtasks": [], "completion": {"result": "Accepted after independent verification: npm test pass (51/51), npm run test:crap pass (all 74 functions below CRAP 8), npm run test:mutation pass (23/23 killed)."}, "close": {"status": "pending_close", "pr": null, "closedAt": null}}
{"schema": "tix/v1", "id": "TIX-000007", "type": "Deliverable", "description": "Slice 3: resume pane-launched sessions from stored metadata", "context": "## Acceptance Criteria\n- GIVEN a prior launch stored metadata and a still-valid child session WHEN the resume tool is invoked THEN the extension restores the pane-backed interaction and returns updated session metadata.\n- GIVEN launch metadata exists across Copilot sessions WHEN the parent session changes THEN resume still works when the underlying session remains valid.\n- GIVEN stored metadata exists but the underlying session is no longer resumable WHEN resume is attempted THEN the extension returns a structured failure explaining what is missing or invalid.\n\n## Spec Context\n- File: /home/jb/.projects/copilot-interactive-subagents/specs/subagents/copilot-interactive-subagents.md\n- Sections: ## Scope, ## Design Decisions, ## Architecture, ### Session State and Resume Model, ### Completion Detection and Summary Contract, ### Key Interfaces, ## Success Criteria, ## Failure Modes / Risks, ## Testing Strategy\n- Subsystem: subagents \u2014 cross-session resume and metadata durability\n- Adjacent: /home/jb/.projects/copilot-interactive-subagents/specs/README.md\n- Note: section references are canonical; do not rely on line numbers in a living spec.\n\n\n## Traceability\n- Shared Key: copilot-interactive-subagents\n- Requirement Refs: None\n- Source: explicit spec ## Traceability\n\n## Must-Haves\nTruths\n- Users can resume a prior pane-launched agent when metadata exists.\n- Resume can work across parent Copilot sessions, not only the current one.\n- Invalid resume targets fail clearly instead of silently disappearing.\n\nArtifacts\n- `.github/extensions/copilot-interactive-subagents/lib/resume.mjs` \u2014 resume orchestration\n- `.github/extensions/copilot-interactive-subagents/lib/state-index.mjs` \u2014 optional project-local index support\n- `test/resume.test.*` \u2014 resume success and invalid-metadata coverage\n\nKey Links\n- `resume.mjs` must reconcile workspace metadata with any optional project-local index before reopening panes.\n- `state.mjs` and `state-index.mjs` must agree on identifiers so resume lookups are deterministic across sessions.\n\n## Implementation Notes\n- Treat resume as best-effort but structured; never invent a fake success when the underlying child session is gone.\n- Persist enough metadata at launch time that resume does not depend on reconstructing state from pane text alone.\n- If project-local indexing is optional, the non-indexed path must still work for current-session metadata.\n\n## Adversarial Test Cases\n- Metadata exists but referenced session identifier is stale -> return explicit invalid-session guidance.\n- Parent session changes and workspace state remains, but project-local index is absent -> resume still works from workspace metadata alone.\n- Resume opens a pane but attach to the underlying session fails -> cleanly report failure and keep metadata intact for debugging.\n\n## Affected Test Files\n- No existing tests affected (new project).\n\n## Test Matrix\n\n### ATDD Scenarios\n- GIVEN valid stored metadata across sessions WHEN resume is requested THEN the extension restores or reattaches the pane-backed session successfully.\n- GIVEN metadata exists but the underlying session is invalid WHEN resume is attempted THEN the extension returns structured diagnostics instead of a false success.\n- GIVEN only workspace metadata exists WHEN the project-local index is absent THEN resume still works where the underlying child session remains valid.\n\n### Unit Tests\n- resume metadata lookup and normalization\n- stale-session / invalid-session classification\n- workspace-first vs optional project-index reconciliation\n- resume result shaping and failure messages\n\n### Integration Tests\n- launch -> persist -> later resume flow across state store, index, mux adapter, and resume service\n- resume with missing or inconsistent metadata\n- pane reopen/reattach failure handling\n\n### End-to-End Tests\n- launch a pane-backed agent, end the parent session, and resume in a later parent session when metadata exists\n- verify invalid resume targets fail with actionable guidance\n\n### Mutation Testing\n- required for metadata lookup, reconciliation, resume eligibility, and failure classification logic\n- target mutation score: >= 80%\n- backend-specific attach wrappers are mutation-advisory if integration coverage is strong\n\n### CRAP Score Evaluation\n- CRAP < 8 required for reconciliation and resume-decision functions\n- any resume path with layered fallback branches should be refactored early if complexity rises\n\n## Resume Contract\n\nResume lookup order:\n1. workspace-backed launch manifest\n2. optional project-local index\n3. structured failure if neither resolves the launch reference\n\nResume eligibility checks:\n- manifest exists and `metadataVersion` is supported\n- launch is not already in a terminal non-resumable state unless inspect-only resume is allowed\n- recorded session or pane reference is still valid enough to attempt reconnect/resume\n\nStable failure codes for this slice:\n- `LAUNCH_NOT_FOUND`\n- `RESUME_TARGET_INVALID`\n- `RESUME_UNSUPPORTED`\n- `RESUME_ATTACH_FAILED`\n\nResume must never return success-shaped output when the underlying target is gone.\n", "assignee": null, "tags": ["copilot-interactive-subagents", "subagents"], "agentState": "Done", "ado": null, "parent": "TIX-000002", "blockedBy": ["TIX-000005"], "subtasks": [], "completion": {"result": "Accepted after independent scoped verification: test/resume.test.mjs pass (7/7), test/quality-gates.test.mjs pass (9/9), node scripts/test-crap.mjs resume pass (all 23 functions below CRAP 8), node scripts/test-mutation.mjs resume pass (6/6 killed)."}, "close": {"status": "pending_close", "pr": null, "closedAt": null}}
{"schema": "tix/v1", "id": "TIX-000008", "type": "Deliverable", "description": "Slice 4: generic tool interface and operator handoff", "context": "## Acceptance Criteria\n- GIVEN Copilot or an external Agent Skill needs subagent orchestration WHEN it inspects the extension surface THEN generic list, single-launch, parallel-launch, and resume tools are available with documented arguments and result shapes.\n- GIVEN an external workflow invokes the generic launch tools WHEN it passes an exact agent identifier and task THEN the extension operates without requiring bundled prompts or packaged workflow-specific agents.\n- GIVEN an operator installs the project WHEN they read the project documentation THEN setup, supported multiplexers, exact-name agent targeting, parallel-write caveats, and resume expectations are documented clearly.\n\n## Spec Context\n- File: /home/jb/.projects/copilot-interactive-subagents/specs/subagents/copilot-interactive-subagents.md\n- Sections: ## Scope, ## Design Decisions, ## Architecture, ### Key Interfaces, ## What Changes, ### Expected Project Layout, ## Success Criteria, ## Failure Modes / Risks, ## Testing Strategy\n- Subsystem: subagents \u2014 reusable orchestration interface and operator handoff\n- Adjacent: /home/jb/.projects/copilot-interactive-subagents/specs/README.md\n- Note: section references are canonical; do not rely on line numbers in a living spec.\n\n\n## Traceability\n- Shared Key: copilot-interactive-subagents\n- Requirement Refs: None\n- Source: explicit spec ## Traceability\n\n## Must-Haves\nTruths\n- Copilot can call the extension's generic orchestration tools during a larger workflow.\n- Skill authors can build workflows on top of the extension without bundled agents.\n- Operators can install and use the extension with clear backend and targeting rules.\n\nArtifacts\n- `.github/extensions/copilot-interactive-subagents/extension.mjs` \u2014 finalized public tool registration\n- `README.md` \u2014 operator-facing setup and usage guide\n- `docs/skills-integration.md` or equivalent \u2014 how Agent Skills should call the tools\n- `test/tool-interface.test.*` \u2014 result-shape and argument validation coverage\n\nKey Links\n- Public tool names and argument shapes in `extension.mjs` must match the documented usage examples exactly.\n- Operator docs must reflect the actual backend-selection and resume behavior implemented by the launch/resume services.\n\n## Implementation Notes\n- Keep the interface generic; do not sneak in planner/worker/reviewer assumptions here.\n- Favor a small stable set of tools over many specialized workflow tools.\n- Document constraints around parallel write-heavy agents explicitly rather than burying them in implementation notes.\n\n## Adversarial Test Cases\n- A future Agent Skill calls the launch tool with malformed arguments -> validation errors must be explicit and documented.\n- Docs claim built-in and custom agents are both supported, but interface validation only handles one path -> tests must catch the mismatch.\n- Tool result shape changes after core implementation -> interface tests and docs must fail/update together.\n\n## Affected Test Files\n- No existing tests affected (new project).\n\n## Test Matrix\n\n### ATDD Scenarios\n- GIVEN Copilot or a higher-level Agent Skill invokes the generic orchestration tools WHEN it provides exact agent names and valid arguments THEN the extension performs the requested launch/resume behavior without bundled prompts.\n- GIVEN malformed arguments or unsupported usage WHEN the public tools are called THEN validation errors are explicit and documented.\n- GIVEN an operator follows the documented setup WHEN they configure the extension THEN supported backends, targeting rules, parallel-write caveats, and resume expectations are clear and accurate.\n\n### Unit Tests\n- public argument validation and normalization\n- public result-shape serialization\n- error-shape consistency for operator-facing failures\n- documentation example contract checks where practical\n\n### Integration Tests\n- extension registration exposes the documented tool surface\n- public tool handlers route correctly into launch, parallel, list, and resume services\n- docs/examples remain aligned with the actual interface contract\n\n### End-to-End Tests\n- Copilot calls the generic launch tool during a larger workflow and receives a usable result\n- operator follows documented setup and successfully launches at least one built-in or custom agent\n\n### Mutation Testing\n- required for public input validation, normalization, and result mapping logic\n- target mutation score: >= 80%\n- documentation-only artifacts are excluded, but code-backed example validators are in scope\n\n### CRAP Score Evaluation\n- CRAP < 8 required for public validation and result-shaping functions\n- keep the public interface layer thin; push branching down into dedicated services when needed\n\n## Initial Public Tool Contracts\n\nRecommended initial public surface:\n- `copilot_subagent_list_agents`\n- `copilot_subagent_launch`\n- `copilot_subagent_parallel`\n- `copilot_subagent_resume`\n- `copilot_subagent_set_title`\n\nMinimum contract expectations:\n- tool names are namespaced to reduce collision risk\n- launch and resume results include stable identifiers (`launchId`, backend/pane/session metadata, terminal status, summary, exitCode)\n- validation failures return stable machine-readable codes plus human-readable guidance\n- docs/examples must match the actual argument and result shapes exactly\n", "assignee": null, "tags": ["copilot-interactive-subagents", "subagents"], "agentState": "Done", "ado": null, "parent": "TIX-000003", "blockedBy": ["TIX-000006", "TIX-000007"], "subtasks": [], "completion": {"result": "Implemented namespaced public tool interface, title helper, skill integration docs, and validated with npm test, coverage, CRAP, and mutation."}, "close": {"status": "pending_close", "pr": null, "closedAt": null}}
{"schema": "tix/v1", "id": "TIX-000009", "type": "Story", "description": "Story: Interactive Subagents v1 \u2014 persistent, interactive, resumable sessions", "context": "", "assignee": null, "tags": ["interactive-subagents-v1"], "agentState": "Todo", "agentStage": "Proposed", "ado": null, "parent": null, "blockedBy": [], "subtasks": [], "completion": null, "close": {"status": "open", "pr": null, "closedAt": null}}
{"schema": "tix/v1", "id": "TIX-000010", "type": "Feature", "description": "Session foundation: manifest v2, launch command, test infrastructure", "context": "", "assignee": null, "tags": ["interactive-subagents-v1", "wave-0-1"], "agentState": "Todo", "agentStage": "Proposed", "ado": null, "parent": "TIX-000009", "blockedBy": [], "subtasks": [], "completion": null, "close": {"status": "open", "pr": null, "closedAt": null}}
{"schema": "tix/v1", "id": "TIX-000011", "type": "Feature", "description": "Core session services: lock, close-pane, summary extraction", "context": "", "assignee": null, "tags": ["interactive-subagents-v1", "wave-2"], "agentState": "Todo", "agentStage": "Proposed", "ado": null, "parent": "TIX-000009", "blockedBy": [], "subtasks": [], "completion": null, "close": {"status": "open", "pr": null, "closedAt": null}}
{"schema": "tix/v1", "id": "TIX-000012", "type": "Feature", "description": "Advanced capabilities: fork and explicit completion", "context": "", "assignee": null, "tags": ["interactive-subagents-v1", "wave-3"], "agentState": "Todo", "agentStage": "Proposed", "ado": null, "parent": "TIX-000009", "blockedBy": [], "subtasks": [], "completion": null, "close": {"status": "open", "pr": null, "closedAt": null}}
{"schema": "tix/v1", "id": "TIX-000013", "type": "Feature", "description": "Orchestration integration: resume, launch, API surface", "context": "", "assignee": null, "tags": ["interactive-subagents-v1", "wave-4"], "agentState": "Todo", "agentStage": "Proposed", "ado": null, "parent": "TIX-000009", "blockedBy": [], "subtasks": [], "completion": null, "close": {"status": "open", "pr": null, "closedAt": null}}
{"schema": "tix/v1", "id": "TIX-000014", "type": "Feature", "description": "End-to-end validation", "context": "", "assignee": null, "tags": ["interactive-subagents-v1", "wave-5"], "agentState": "Todo", "agentStage": "Proposed", "ado": null, "parent": "TIX-000009", "blockedBy": [], "subtasks": [], "completion": null, "close": {"status": "open", "pr": null, "closedAt": null}}
{"schema": "tix/v1", "id": "TIX-000015", "type": "Deliverable", "description": "Slice 0: Test infrastructure and shared fixtures", "context": "- **Shared Key**: interactive-subagents-v1\n- **Spec Path**: specs/subagents/interactive-subagents-v1.md\n- **Spec Lines**: 13-16 (Wave 0), 769-807 (Testing Strategy)\n\n## Acceptance Criteria\n\nGIVEN no test directory structure exists\nWHEN this slice is implemented\nTHEN test/unit/, test/integration/, test/e2e/ directories exist\n\nGIVEN no shared fixture factories\nWHEN this slice is implemented\nTHEN test/helpers/fixtures.mjs exports: createSessionDir, createEventsJsonl, createManifestV2, createLockfile\n\nGIVEN no split test scripts in package.json\nWHEN npm run test:unit is executed\nTHEN only test/unit/*.test.mjs files run\n\nGIVEN existing tests at test/*.test.mjs\nWHEN npm test is executed\nTHEN both test/*.test.mjs AND test/unit/*.test.mjs files run (existing tests not broken)\n\n## Must-Haves\n- **Truths**: test dirs exist; fixtures export 4 factories; package.json has test:unit, test:integration, test:e2e scripts; existing tests still pass\n- **Artifacts**: test/unit/.gitkeep, test/integration/.gitkeep, test/e2e/.gitkeep, test/helpers/fixtures.mjs, package.json (updated)\n- **Key Links**: fixtures.mjs is imported by ALL subsequent test files \u2014 get the API right\n\n## Implementation Notes\n- createManifestV2(overrides) must produce complete v2 manifests with sensible defaults (see spec lines 349-374 for schema)\n- createSessionDir(t, opts) should use t.after() for cleanup registration (node:test cleanup)\n- createEventsJsonl(events) takes array of event objects, returns JSONL string\n- Do NOT move existing test files from test/ \u2014 they stay where they are\n- The test script in package.json must glob BOTH locations: test/*.test.mjs AND test/unit/*.test.mjs\n\n## Adversarial Test Cases\n- Fixture factory with no overrides produces valid manifest (all required fields present)\n- createSessionDir cleanup actually removes temp dir even if test throws\n- createEventsJsonl handles empty array (returns empty string)\n\n## CRAP / Quality Gates\n- package.json MUST include test:crap and test:mutation scripts (they already exist in spec)\n- After all scripts are updated, run npm run test:crap to verify baseline CRAP scores are preserved\n- test:coverage script must include both test/*.test.mjs AND test/unit/*.test.mjs so CRAP computation covers new files\n", "assignee": null, "tags": ["interactive-subagents-v1", "wave-0"], "agentState": "Todo", "agentStage": "Proposed", "ado": null, "parent": "TIX-000010", "blockedBy": [], "subtasks": [], "completion": null, "close": {"status": "open", "pr": null, "closedAt": null}}
{"schema": "tix/v1", "id": "TIX-000016", "type": "Deliverable", "description": "Slice 1: Manifest v2 schema with session identity fields", "context": "- **Shared Key**: interactive-subagents-v1\n- **Spec Path**: specs/subagents/interactive-subagents-v1.md\n- **Spec Lines**: 343-385 (Manifest v2 Schema), 100-108 (cmux compat)\n\n## Acceptance Criteria\n\nGIVEN METADATA_VERSION is currently 1\nWHEN this slice is implemented\nTHEN METADATA_VERSION is 2 in lib/state.mjs\n\nGIVEN createLaunchRecord is called with v1 params\nWHEN a manifest is created\nTHEN it contains copilotSessionId (string), interactive (boolean), fork (object|null), closePaneOnCompletion (boolean), eventsBaseline (number|null)\n\nGIVEN a cmux backend launch\nWHEN manifest is created\nTHEN copilotSessionId is null, interactive is false, fork is null, closePaneOnCompletion is false, eventsBaseline is null\n\nGIVEN existing tests assert metadataVersion: 1\nWHEN this slice is implemented\nTHEN those tests are updated to assert metadataVersion: 2\n\nGIVEN a manifest is written to disk and read back\nWHEN JSON round-trip occurs\nTHEN all v2 fields survive serialization (including null values)\n\n## Must-Haves\n- **Truths**: METADATA_VERSION === 2; createLaunchRecord produces v2 shape; all existing tests updated; cmux gets null defaults\n- **Artifacts**: lib/state.mjs (updated), test/state-store.test.mjs (updated)\n- **Key Links**: createLaunchRecord is called by lib/launch.mjs \u2014 the function signature must remain backward-compatible (new params optional with defaults)\n\n## Implementation Notes\n- Do NOT rename createLaunchRecord \u2014 all existing call sites use this name\n- New params should have defaults: interactive=false, fork=null, closePaneOnCompletion=true, copilotSessionId=null, eventsBaseline=null\n- Search for existing tests: grep -rn \"metadataVersion.*1\" test/\n- The status field gains new values: \"interactive\" (in addition to existing pending/running/success/failure/timeout/cancelled)\n\n## Adversarial Test Cases\n- Manifest created with NO new params still produces valid v2 (defaults applied)\n- fork field with nested object survives JSON round-trip\n- eventsBaseline: 0 is preserved (not coerced to null)\n\n## CRAP / Quality Gates\n- lib/state.mjs is ALREADY in DETERMINISTIC_LOGIC_TARGETS \u2014 CRAP score must stay < 8 after changes\n- Add targeted mutant for metadataVersion: 2 (mutate to 1 or 0, verify test catches it)\n- Add targeted mutant for copilotSessionId default (mutate null to undefined, verify test catches)\n- Run npm run test:crap and npm run test:mutation after implementation \u2014 both must pass\n\n## Cold-Start Agent Guide\n\n### DI Pattern (copy this)\nAll new modules follow the dependency-injection pattern. See `lib/launch.mjs:22-59` for the canonical example:\n```javascript\nexport function myService(args, services = {}) {\n  const { readFileSync = fs.readFileSync, writeFileSync = fs.writeFileSync } = services;\n  // ... use injected operations\n}\n```\nTests inject stubs: `myService(args, { readFileSync: () => '...' })`\n\n### Test Harness (copy this)\nImport modules via red-harness for export validation:\n```javascript\nimport { importProjectModule } from './helpers/red-harness.mjs';\nconst { myFunction } = await importProjectModule('lib/my-module.mjs');\n```\nSee `test/resume.test.mjs:69-78` for a complete example with DI stubs.\n\n### Verification\n\u26a0\ufe0f **Blocked by TIX-000015**: `test/unit/` directory and `npm run test:unit` do not exist yet.\nUntil TIX-000015 lands, unit tests for this slice cannot be run via the standard workflow.\nIf working in parallel, run the test file directly: `node --test test/unit/<filename>.test.mjs`\n", "assignee": null, "tags": ["interactive-subagents-v1", "wave-1"], "agentState": "Todo", "agentStage": "Proposed", "ado": null, "parent": "TIX-000010", "blockedBy": ["TIX-000015"], "subtasks": [], "completion": null, "close": {"status": "open", "pr": null, "closedAt": null}}
{"schema": "tix/v1", "id": "TIX-000017", "type": "Deliverable", "description": "Slice 2: Launch command builder with --resume, -i/-p, and safe transport", "context": "- **Shared Key**: interactive-subagents-v1\n- **Spec Path**: specs/subagents/interactive-subagents-v1.md\n- **Spec Lines**: 314-340 (Launch Command Builder), 147 (safe transport invariant), 157-159 (flag preservation invariant)\n\n## Acceptance Criteria\n\nGIVEN interactive is false (default)\nWHEN createDefaultAgentLaunchCommand is called\nTHEN output contains -p flag and -s flag (existing behavior preserved)\n\nGIVEN interactive is true\nWHEN createDefaultAgentLaunchCommand is called\nTHEN output contains -i flag and does NOT contain -s flag\n\nGIVEN any launch (interactive or autonomous)\nWHEN createDefaultAgentLaunchCommand is called\nTHEN output contains --resume=<UUID> with a valid RFC 4122 UUID\n\nGIVEN a task containing shell metacharacters (quotes, $, backticks, newlines)\nWHEN createDefaultAgentLaunchCommand is called\nTHEN task content is transported via base64 env var COPILOT_SUBAGENT_TASK_B64 (never raw interpolation)\n\nGIVEN existing flags (--allow-all-tools, --allow-all-paths, --allow-all-urls, --no-ask-user)\nWHEN createDefaultAgentLaunchCommand is called with v1 params\nTHEN all existing flags are preserved in the output\n\n## Must-Haves\n- **Truths**: -i/-p selection works; --resume always present; safe transport; existing flags preserved; returns single shell string\n- **Artifacts**: extension.mjs (updated), test/unit/launch-command.test.mjs (new)\n- **Key Links**: This function is called by lib/launch.mjs \u2014 return type must remain single shell string (not array)\n\n## Implementation Notes\n- Function currently at extension.mjs ~line 809-836\n- Return type: single shell command string (NOT array) \u2014 same contract as current code\n- For interactive: the sentinel wrapper (node -e \"...spawnSync...\") is still used but spawns copilot with -i instead of -p\n- copilotSessionId should be accepted as a param and used in --resume=<id>\n- The base64 transport pattern already exists \u2014 extend it, do not replace\n\n## Adversarial Test Cases\n- Task with backticks and $() does not cause shell injection\n- Empty task string still produces valid command\n- Interactive + custom agent identifier includes both --agent and -i flags\n\n## CRAP / Quality Gates\n- extension.mjs is ALREADY in DETERMINISTIC_LOGIC_TARGETS \u2014 CRAP < 8 after changes\n- Add targeted mutant: --resume flag omitted (verify test catches missing --resume)\n- Add targeted mutant: -i replaced with -p when interactive: true (verify test catches wrong flag)\n- Add targeted mutant: -s flag present when interactive: true (verify test catches -s leak)\n- Run npm run test:crap and npm run test:mutation after implementation\n\n## Cold-Start Agent Guide\n\n### DI Pattern (copy this)\nAll new modules follow the dependency-injection pattern. See `lib/launch.mjs:22-59` for the canonical example:\n```javascript\nexport function myService(args, services = {}) {\n  const { readFileSync = fs.readFileSync, writeFileSync = fs.writeFileSync } = services;\n  // ... use injected operations\n}\n```\nTests inject stubs: `myService(args, { readFileSync: () => '...' })`\n\n### Test Harness (copy this)\nImport modules via red-harness for export validation:\n```javascript\nimport { importProjectModule } from './helpers/red-harness.mjs';\nconst { myFunction } = await importProjectModule('lib/my-module.mjs');\n```\nSee `test/resume.test.mjs:69-78` for a complete example with DI stubs.\n\n### Verification\n\u26a0\ufe0f **Blocked by TIX-000015**: `test/unit/` directory and `npm run test:unit` do not exist yet.\nUntil TIX-000015 lands, unit tests for this slice cannot be run via the standard workflow.\nIf working in parallel, run the test file directly: `node --test test/unit/<filename>.test.mjs`\n", "assignee": null, "tags": ["interactive-subagents-v1", "wave-1"], "agentState": "Todo", "agentStage": "Proposed", "ado": null, "parent": "TIX-000010", "blockedBy": ["TIX-000015"], "subtasks": [], "completion": null, "close": {"status": "open", "pr": null, "closedAt": null}}
{"schema": "tix/v1", "id": "TIX-000018", "type": "Deliverable", "description": "Slice 3: Per-session lockfile service", "context": "## Traceability\n- **Shared Key**: interactive-subagents-v1\n- **Spec Path**: specs/subagents/interactive-subagents-v1.md\n- **Spec Lines**: 388-413 (sessionLock), 152-153 (lockfile invariant), 164-165 (caller-owned locks invariant), 720-721 (locking details)\n\n## Acceptance Criteria\n\nGIVEN no lock exists for a copilotSessionId\nWHEN acquireLock(copilotSessionId) is called\nTHEN lock is acquired and returns { release: () => void }\n\nGIVEN a lock is held for a copilotSessionId\nWHEN a second acquireLock(copilotSessionId) is called\nTHEN it throws a structured error with code SESSION_ACTIVE\n\nGIVEN a lock is held\nWHEN release() is called\nTHEN the lockfile is removed and subsequent acquireLock succeeds\n\nGIVEN a process holding a lock crashes\nWHEN the process exits\nTHEN the lockfile is cleaned up via process exit handler\n\nGIVEN the locks directory does not exist\nWHEN acquireLock is called for the first time\nTHEN the directory is created automatically\n\n## Must-Haves\n- **Truths**: atomic O_CREAT|O_EXCL creation; SESSION_ACTIVE on contention; release is idempotent; cleanup on process exit\n- **Artifacts**: lib/session-lock.mjs (new), test/unit/session-lock.test.mjs (new)\n- **Key Links**: Used by resume (D9), fork (D7), and launch orchestration (D10) \u2014 must be robust\n\n## Implementation Notes\n- Lockfile path: .copilot-interactive-subagents/locks/<copilotSessionId>.lock\n- Use fs.openSync with O_CREAT | O_EXCL for atomic creation (not writeFileSync)\n- release() should be idempotent \u2014 no error if lockfile already removed\n- Register process.on(\"exit\") cleanup as safety net but caller is responsible for explicit release\n- Follow DI pattern: fs operations injectable via services param\n\n## Adversarial Test Cases\n- Two concurrent acquireLock calls \u2014 exactly one succeeds, one throws SESSION_ACTIVE\n- release() called twice \u2014 no error on second call\n- Lock acquired, process.exit handler removes file (simulate via manual cleanup check)\n\n## CRAP / Quality Gates\n- lib/session-lock.mjs is a NEW module \u2014 add to DETERMINISTIC_LOGIC_TARGETS in scripts/quality/targets.mjs\n- CRAP < 8 required for all exported functions (acquireLock, releaseLock)\n- Add targeted mutant: O_EXCL flag removed from openSync (verify test catches non-atomic creation)\n- Add targeted mutant: process.on(\"exit\") handler removed (verify test catches missing cleanup)\n- Add targeted mutant: release() throws on missing lockfile instead of being idempotent (verify test catches)\n- Run npm run test:crap and npm run test:mutation after implementation\n## Cold-Start Agent Guide\n\n### DI Pattern (copy this)\nAll new modules follow the dependency-injection pattern. See `lib/launch.mjs:22-59` for the canonical example:\n```javascript\nexport function myService(args, services = {}) {\n  const { readFileSync = fs.readFileSync, writeFileSync = fs.writeFileSync } = services;\n  // ... use injected operations\n}\n```\nTests inject stubs: `myService(args, { readFileSync: () => '...' })`\n\n### Test Harness (copy this)\nImport modules via red-harness for export validation:\n```javascript\nimport { importProjectModule } from './helpers/red-harness.mjs';\nconst { myFunction } = await importProjectModule('lib/my-module.mjs');\n```\nSee `test/resume.test.mjs:69-78` for a complete example with DI stubs.\n\n### Verification\n\u26a0\ufe0f **Blocked by TIX-000015**: `test/unit/` directory and `npm run test:unit` do not exist yet.\nUntil TIX-000015 lands, unit tests for this slice cannot be run via the standard workflow.\nIf working in parallel, run the test file directly: `node --test test/unit/<filename>.test.mjs`\n", "assignee": null, "tags": ["interactive-subagents-v1", "wave-2"], "agentState": "Todo", "agentStage": "Proposed", "ado": null, "parent": "TIX-000011", "blockedBy": ["TIX-000015"], "subtasks": [], "completion": null, "close": {"status": "open", "pr": null, "closedAt": null}}
{"schema": "tix/v1", "id": "TIX-000019", "type": "Deliverable", "description": "Slice 4: Close pane service with backend support", "context": "## Traceability\n- **Shared Key**: interactive-subagents-v1\n- **Spec Path**: specs/subagents/interactive-subagents-v1.md\n- **Spec Lines**: 416-438 (closePane service), 154-155 (extract\u2192manifest\u2192close invariant)\n\n## Acceptance Criteria\n\nGIVEN a tmux backend and valid paneId\nWHEN closePane is called\nTHEN it runs: tmux kill-pane -t <paneId>\n\nGIVEN a zellij backend and valid paneId\nWHEN closePane is called\nTHEN it runs: zellij action close-pane with ZELLIJ_PANE_ID env var\n\nGIVEN pane already gone (not found)\nWHEN closePane is called\nTHEN it succeeds silently (idempotent)\n\nGIVEN an unknown backend name\nWHEN closePane is called\nTHEN it throws a structured error\n\n## Must-Haves\n- **Truths**: correct command per backend; idempotent on missing pane; structured error on unknown backend\n- **Artifacts**: lib/close-pane.mjs (new), test/unit/close-pane.test.mjs (new)\n- **Key Links**: Called by lib/launch.mjs after summary extraction \u2014 must be called AFTER extract, never before\n\n## Implementation Notes\n- Follow DI pattern: spawnSync injectable via services param\n- For tmux: spawnSync(\"tmux\", [\"kill-pane\", \"-t\", paneId])\n- For zellij: spawnSync(\"zellij\", [\"action\", \"close-pane\"], { env: { ...process.env, ZELLIJ_PANE_ID: paneId } })\n- \"Pane not found\" detection: check stderr/exit code for known patterns (\"pane not found\", exit code 1)\n\n## Adversarial Test Cases\n- closePane with paneId containing special chars (tmux uses % prefix)\n- Backend \"cmux\" \u2014 should throw (cmux panes are not managed by this service)\n\n## CRAP / Quality Gates\n- lib/close-pane.mjs is a NEW module \u2014 add to DETERMINISTIC_LOGIC_TARGETS in scripts/quality/targets.mjs\n- CRAP < 8 required for all exported functions (closePane)\n- Add targeted mutant: kill-pane replaced with send-keys (verify test catches wrong tmux command)\n- Add targeted mutant: pane validation skipped \u2014 closePane called on non-existent pane (verify error handling)\n- Add targeted mutant: delay before kill-pane removed (verify test catches missing grace period if specified)\n- Run npm run test:crap and npm run test:mutation after implementation\n## Cold-Start Agent Guide\n\n### DI Pattern (copy this)\nAll new modules follow the dependency-injection pattern. See `lib/launch.mjs:22-59` for the canonical example:\n```javascript\nexport function myService(args, services = {}) {\n  const { readFileSync = fs.readFileSync, writeFileSync = fs.writeFileSync } = services;\n  // ... use injected operations\n}\n```\nTests inject stubs: `myService(args, { readFileSync: () => '...' })`\n\n### Test Harness (copy this)\nImport modules via red-harness for export validation:\n```javascript\nimport { importProjectModule } from './helpers/red-harness.mjs';\nconst { myFunction } = await importProjectModule('lib/my-module.mjs');\n```\nSee `test/resume.test.mjs:69-78` for a complete example with DI stubs.\n\n### Verification\n\u26a0\ufe0f **Blocked by TIX-000015**: `test/unit/` directory and `npm run test:unit` do not exist yet.\nUntil TIX-000015 lands, unit tests for this slice cannot be run via the standard workflow.\nIf working in parallel, run the test file directly: `node --test test/unit/<filename>.test.mjs`\n", "assignee": null, "tags": ["interactive-subagents-v1", "wave-2"], "agentState": "Todo", "agentStage": "Proposed", "ado": null, "parent": "TIX-000011", "blockedBy": ["TIX-000015"], "subtasks": [], "completion": null, "close": {"status": "open", "pr": null, "closedAt": null}}
{"schema": "tix/v1", "id": "TIX-000020", "type": "Deliverable", "description": "Slice 5: Session summary extraction from events.jsonl", "context": "## Traceability\n- **Shared Key**: interactive-subagents-v1\n- **Spec Path**: specs/subagents/interactive-subagents-v1.md\n- **Spec Lines**: 441-479 (extractSessionSummary), 160-161 (graceful degradation invariant)\n\n## Acceptance Criteria\n\nGIVEN a session with events.jsonl containing assistant.message events\nWHEN extractSessionSummary is called\nTHEN it returns the last assistant.message content as summary\n\nGIVEN sinceEventIndex is provided\nWHEN extractSessionSummary is called\nTHEN only events after that index are considered (delta mode)\n\nGIVEN sinceEventIndex is set and no new assistant.message exists after that index\nWHEN extractSessionSummary is called\nTHEN summary is null (does NOT reuse stale summary)\n\nGIVEN session directory does not exist\nWHEN extractSessionSummary is called\nTHEN it returns { summary: null, source: \"fallback\", lastEventIndex: 0 } (no crash)\n\nGIVEN events.jsonl has a truncated trailing line\nWHEN extractSessionSummary is called\nTHEN the partial line is ignored and valid events are processed\n\nGIVEN no assistant.message in events.jsonl and sinceEventIndex is not set\nWHEN extractSessionSummary is called\nTHEN it falls back to workspace.yaml summary field\n\n## Must-Haves\n- **Truths**: events.jsonl parsing works; delta mode via sinceEventIndex; graceful degradation; workspace.yaml fallback\n- **Artifacts**: lib/summary.mjs (updated \u2014 new export), test/unit/summary-extraction.test.mjs (new)\n- **Key Links**: extractSessionSummary coexists with existing extractLaunchSummary \u2014 do NOT modify/remove existing exports\n\n## Implementation Notes\n- New EXPORT added to lib/summary.mjs \u2014 existing extractLaunchSummary, waitForLaunchCompletion, mapExitState unchanged\n- events.jsonl path: ~/.copilot/session-state/<copilotSessionId>/events.jsonl\n- JSONL = one JSON object per line. Parse line-by-line, skip invalid lines (tolerant)\n- assistant.message events have shape: {\"type\":\"assistant.message\",\"data\":{\"content\":\"...\"}}\n- Return shape: { summary: string|null, source: \"events.jsonl\"|\"workspace.yaml\"|\"fallback\", lastEventIndex: number }\n- workspace.yaml fallback: parse YAML, look for summary field\n- Follow DI pattern: fs.readFileSync injectable via services\n\n## Adversarial Test Cases\n- events.jsonl with 0 bytes \u2014 returns null gracefully\n- events.jsonl with mixed event types \u2014 only assistant.message considered\n- Delta extraction with sinceEventIndex=5 on a 3-event file \u2014 returns null (index beyond range)\n\n## CRAP / Quality Gates\n- lib/summary.mjs is ALREADY in DETERMINISTIC_LOGIC_TARGETS \u2014 CRAP < 8 after changes\n- Add targeted mutant: events.jsonl path hardcoded instead of using session directory (verify test catches)\n- Add targeted mutant: JSON parse error swallowed silently instead of returning fallback (verify test catches)\n- Add targeted mutant: assistant_response field read instead of correct event field (verify test catches wrong extraction)\n- Run npm run test:crap and npm run test:mutation after implementation\n## Cold-Start Agent Guide\n\n### DI Pattern (copy this)\nAll new modules follow the dependency-injection pattern. See `lib/launch.mjs:22-59` for the canonical example:\n```javascript\nexport function myService(args, services = {}) {\n  const { readFileSync = fs.readFileSync, writeFileSync = fs.writeFileSync } = services;\n  // ... use injected operations\n}\n```\nTests inject stubs: `myService(args, { readFileSync: () => '...' })`\n\n### Test Harness (copy this)\nImport modules via red-harness for export validation:\n```javascript\nimport { importProjectModule } from './helpers/red-harness.mjs';\nconst { myFunction } = await importProjectModule('lib/my-module.mjs');\n```\nSee `test/resume.test.mjs:69-78` for a complete example with DI stubs.\n\n### Verification\n\u26a0\ufe0f **Blocked by TIX-000015**: `test/unit/` directory and `npm run test:unit` do not exist yet.\nUntil TIX-000015 lands, unit tests for this slice cannot be run via the standard workflow.\nIf working in parallel, run the test file directly: `node --test test/unit/<filename>.test.mjs`\n\n### Path Construction\nUse `path.join(os.homedir(), '.copilot', 'session-state', copilotSessionId, ...)` \u2014 NOT `~/.copilot/...`.\nNode path APIs do not expand `~`.\n### copilotSessionId vs sessionId\nAccept `copilotSessionId` as a direct parameter. Do NOT use the manifest's legacy `sessionId` field.\n`copilotSessionId` is the UUID from `copilot --resume=<UUID>` and maps to `~/.copilot/session-state/<UUID>/`.\n\n### YAML Strategy\nDo NOT add a YAML parser dependency. Extract `summary:` from workspace.yaml via simple regex:\n```javascript\nconst match = content.match(/^summary:\\s*(.+)$/m);\n```\n\n### Fixture Examples\n**events.jsonl** (3-line sample for tests):\n```jsonl\n{\"type\":\"system\",\"data\":{\"content\":\"Session started\"}}\n{\"type\":\"user.message\",\"data\":{\"content\":\"Fix the auth bug\"}}\n{\"type\":\"assistant.message\",\"data\":{\"content\":\"I fixed the authentication issue by updating the token refresh logic.\"}}\n```\n\n**workspace.yaml** (minimal sample for fallback test):\n```yaml\nsummary: Fixed authentication token refresh logic\ncreated_at: 2025-01-15T10:00:00Z\n```\n\n### Delta Index Semantics\n- `sinceEventIndex` is the zero-based count of successfully parsed complete events already seen\n- `lastEventIndex` is the total count of successfully parsed complete events in the file\n- Invalid/truncated lines are skipped and do NOT increment the count\n- Example: file has 10 lines, 2 are truncated \u2192 lastEventIndex = 8\n\n### Blocker Context\nBlocked by TIX-000015 which creates: `test/unit/` directory, `npm run test:unit` script, fixture helpers, and red-harness integration for the unit test subdirectory.", "assignee": null, "tags": ["interactive-subagents-v1", "wave-2"], "agentState": "Todo", "agentStage": "Proposed", "ado": null, "parent": "TIX-000011", "blockedBy": ["TIX-000015"], "subtasks": [], "completion": null, "close": {"status": "open", "pr": null, "closedAt": null}}
{"schema": "tix/v1", "id": "TIX-000021", "type": "Deliverable", "description": "Slice 6: Fork session service with dir copy and yaml update", "context": "## Traceability\n- **Shared Key**: interactive-subagents-v1\n- **Spec Path**: specs/subagents/interactive-subagents-v1.md\n- **Spec Lines**: 482-512 (forkSession), 231-237 (Fork launch flow)\n\n## Acceptance Criteria\n\nGIVEN a valid parent copilotSessionId with a session directory\nWHEN forkSession is called\nTHEN a new directory at ~/.copilot/session-state/<forkUUID>/ contains a copy of parent events.jsonl\n\nGIVEN a fork is created\nWHEN workspace.yaml in the fork is read\nTHEN the id field is the NEW fork UUID (not the parent)\n\nGIVEN a fork is created\nWHEN the parent session directory is inspected\nTHEN events.jsonl and workspace.yaml are completely untouched\n\nGIVEN disk is full during copy\nWHEN forkSession is called\nTHEN it returns structured error FORK_FAILED and removes any partial temp directory\n\nGIVEN the generated fork UUID collides with an existing session directory\nWHEN forkSession is called\nTHEN it regenerates and retries (or returns error)\n\n## Must-Haves\n- **Truths**: fork dir exists with parent content; yaml id updated; parent untouched; cleanup on failure\n- **Artifacts**: lib/fork-session.mjs (new), test/unit/fork-session.test.mjs (new)\n- **Key Links**: Requires session lock (D4) \u2014 must acquire lock on parent copilotSessionId before copying\n\n## Implementation Notes\n- Use temp dir + rename pattern for atomic copy: cp -r parent/ temp/ \u2192 update yaml in temp/ \u2192 rename temp/ \u2192 final/\n- Requires sessionLock \u2014 acquire lock on parent before copy, release after\n- crypto.randomUUID() for fork UUID generation\n- workspace.yaml is YAML \u2014 use simple regex or line replacement for id field (avoid adding yaml parser dep)\n- Follow DI pattern: fs, child_process, sessionLock injectable via services\n\n## Adversarial Test Cases\n- Fork from parent with 100+ events \u2014 all events present in fork\n- Concurrent fork from same parent \u2014 second fork gets SESSION_ACTIVE (lock held)\n- Parent session dir with no workspace.yaml \u2014 still copies events, yaml update skipped gracefully\n\n## CRAP / Quality Gates\n- lib/fork-session.mjs is a NEW module \u2014 add to DETERMINISTIC_LOGIC_TARGETS in scripts/quality/targets.mjs\n- CRAP < 8 required for all exported functions (forkSession)\n- Add targeted mutant: new UUID not generated \u2014 reuses parent session ID (verify test catches ID collision)\n- Add targeted mutant: parent session lock not checked before forking (verify test catches unlocked parent)\n- Add targeted mutant: state directory not copied/initialized for forked session (verify test catches missing state)\n- Run npm run test:crap and npm run test:mutation after implementation\n## Cold-Start Agent Guide\n\n### DI Pattern (copy this)\nAll new modules follow the dependency-injection pattern. See `lib/launch.mjs:22-59` for the canonical example:\n```javascript\nexport function myService(args, services = {}) {\n  const { readFileSync = fs.readFileSync, writeFileSync = fs.writeFileSync } = services;\n  // ... use injected operations\n}\n```\nTests inject stubs: `myService(args, { readFileSync: () => '...' })`\n\n### Test Harness (copy this)\nImport modules via red-harness for export validation:\n```javascript\nimport { importProjectModule } from './helpers/red-harness.mjs';\nconst { myFunction } = await importProjectModule('lib/my-module.mjs');\n```\nSee `test/resume.test.mjs:69-78` for a complete example with DI stubs.\n\n### Verification\n\u26a0\ufe0f **Blocked by TIX-000015**: `test/unit/` directory and `npm run test:unit` do not exist yet.\nUntil TIX-000015 lands, unit tests for this slice cannot be run via the standard workflow.\nIf working in parallel, run the test file directly: `node --test test/unit/<filename>.test.mjs`\n\n### Path Construction\nUse `path.join(os.homedir(), '.copilot', 'session-state', copilotSessionId, ...)` \u2014 NOT `~/.copilot/...`.\nNode path APIs do not expand `~`.\n### copilotSessionId vs sessionId\nAccept `copilotSessionId` as a direct parameter. Do NOT use the manifest's legacy `sessionId` field.\n`copilotSessionId` is the UUID from `copilot --resume=<UUID>` and maps to `~/.copilot/session-state/<UUID>/`.", "assignee": null, "tags": ["interactive-subagents-v1", "wave-3"], "agentState": "Todo", "agentStage": "Proposed", "ado": null, "parent": "TIX-000012", "blockedBy": ["TIX-000018"], "subtasks": [], "completion": null, "close": {"status": "open", "pr": null, "closedAt": null}}
{"schema": "tix/v1", "id": "TIX-000022", "type": "Deliverable", "description": "Slice 7: subagent_done tool with signal file completion", "context": "## Traceability\n- **Shared Key**: interactive-subagents-v1\n- **Spec Path**: specs/subagents/interactive-subagents-v1.md\n- **Spec Lines**: 515-559 (subagent_done tool)\n\n## Acceptance Criteria\n\nGIVEN a copilot session with a real TTY (tmux pane)\nWHEN the extension loads\nTHEN subagent_done tool is registered with name \"subagent_done\" and empty parameters\n\nGIVEN the subagent_done tool is called with a copilotSessionId\nWHEN execute runs\nTHEN a signal file is written to .copilot-interactive-subagents/done/<copilotSessionId>\n\nGIVEN the subagent_done tool is called\nWHEN execute returns\nTHEN it returns { ok: true, message: \"Task marked complete. Session ending.\" }\n\nGIVEN the done/ directory does not exist\nWHEN subagent_done is called for the first time\nTHEN the directory is created automatically (mkdirSync recursive)\n\n## Must-Haves\n- **Truths**: tool registered; signal file written; returns ok result; dir auto-created\n- **Artifacts**: extension.mjs (updated \u2014 tool registration), test/unit/subagent-done.test.mjs (new)\n- **Key Links**: Signal file is read by parent monitoring during lifecycle guards \u2014 path must match: .copilot-interactive-subagents/done/<copilotSessionId>\n\n## Implementation Notes\n- IMPORTANT: The extension sidecar is a SEPARATE process from copilot. process.exit(0) kills the sidecar, NOT copilot. Do NOT use process.exit.\n- Tool description must instruct the model: \"Put your final summary in your last message BEFORE calling this tool.\"\n- Signal file content: timestamp string (Date.now().toString())\n- copilotSessionId must be discoverable by the sidecar \u2014 pass via environment variable or derive from SESSION_ID\n- Follow existing joinSession({ tools }) pattern in extension.mjs for tool registration\n\n## Adversarial Test Cases\n- Two calls to subagent_done with same copilotSessionId \u2014 second overwrites signal file (no error)\n- Signal file content is readable and contains valid timestamp\n\n## CRAP / Quality Gates\n- extension.mjs is ALREADY in DETERMINISTIC_LOGIC_TARGETS \u2014 CRAP < 8 after changes\n- Add targeted mutant: signal file path wrong (uses session dir instead of locks dir) (verify test catches)\n- Add targeted mutant: done signal written before session actually completes (verify test catches premature signal)\n- Add targeted mutant: signal file content empty instead of containing exit status (verify test catches missing metadata)\n- Run npm run test:crap and npm run test:mutation after implementation\n## Cold-Start Agent Guide\n\n### DI Pattern (copy this)\nAll new modules follow the dependency-injection pattern. See `lib/launch.mjs:22-59` for the canonical example:\n```javascript\nexport function myService(args, services = {}) {\n  const { readFileSync = fs.readFileSync, writeFileSync = fs.writeFileSync } = services;\n  // ... use injected operations\n}\n```\nTests inject stubs: `myService(args, { readFileSync: () => '...' })`\n\n### Test Harness (copy this)\nImport modules via red-harness for export validation:\n```javascript\nimport { importProjectModule } from './helpers/red-harness.mjs';\nconst { myFunction } = await importProjectModule('lib/my-module.mjs');\n```\nSee `test/resume.test.mjs:69-78` for a complete example with DI stubs.\n\n### Verification\n\u26a0\ufe0f **Blocked by TIX-000015**: `test/unit/` directory and `npm run test:unit` do not exist yet.\nUntil TIX-000015 lands, unit tests for this slice cannot be run via the standard workflow.\nIf working in parallel, run the test file directly: `node --test test/unit/<filename>.test.mjs`\n", "assignee": null, "tags": ["interactive-subagents-v1", "wave-3"], "agentState": "Todo", "agentStage": "Proposed", "ado": null, "parent": "TIX-000012", "blockedBy": ["TIX-000015"], "subtasks": [], "completion": null, "close": {"status": "open", "pr": null, "closedAt": null}}
{"schema": "tix/v1", "id": "TIX-000023", "type": "Deliverable", "description": "Slice 8: Resume flow rewrite with new pane and session lock", "context": "## Traceability\n- **Shared Key**: interactive-subagents-v1\n- **Spec Path**: specs/subagents/interactive-subagents-v1.md\n- **Spec Lines**: 562-627 (Resume Flow), 239-248 (Resume data flow), 708-732 (Lifecycle Guards)\n\n## Acceptance Criteria\n\nGIVEN a completed launch with a manifest\nWHEN copilot_subagent_resume is called with launchId\nTHEN a NEW pane is created (not reattached to old pane)\n\nGIVEN a resumed session\nWHEN the copilot command is sent to the new pane\nTHEN it includes --resume=<copilotSessionId> and -i flag\n\nGIVEN a session that is still running (pane alive, copilot process active)\nWHEN resume is called\nTHEN it returns { ok: false, code: \"SESSION_ACTIVE\", message: \"...\" }\n\nGIVEN resume with a task string\nWHEN copilot is launched in the new pane\nTHEN task is included via -i \"task\" with safe base64 transport\n\nGIVEN resume with awaitCompletion: true\nWHEN copilot process exits in the new pane\nTHEN summary is extracted (delta via eventsBaseline), manifest transitions to terminal, response includes summary\n\nGIVEN resume with awaitCompletion: false (default)\nWHEN pane is created and copilot launched\nTHEN response is returned immediately with status \"interactive\"\n\nGIVEN a stale manifest (status \"running\" but pane gone)\nWHEN resume is called\nTHEN manifest is reconciled to terminal state first, then resume proceeds\n\n## Must-Haves\n- **Truths**: new pane created; --resume in command; SESSION_ACTIVE on active; eventsBaseline recorded; two-phase locking\n- **Artifacts**: lib/resume.mjs (rewritten), test/resume.test.mjs (updated)\n- **Key Links**: Depends on sessionLock (D4), extractSessionSummary (D6). Two-phase locking: acquire\u2192setup\u2192release, then on completion: re-acquire\u2192extract\u2192transition\u2192release\n\n## Implementation Notes\n- This is a REWRITE of lib/resume.mjs \u2014 current code does pane-reattach, new code creates new panes\n- eventsBaseline: count lines in events.jsonl before launch, store in manifest\n- Locking: acquire before pane creation, release after manifest update. If awaitCompletion, re-acquire lock for terminal transition.\n- Stale manifest reconciliation: use probeSessionLiveness to check pane/process state before rejecting\n- Resume always uses -i flag (interactive) even for originally autonomous sessions\n- resumePointer in response: { launchId, copilotSessionId, backend, paneId }\n\n## Adversarial Test Cases\n- Resume of session where pane crashed mid-execution \u2014 stale reconciliation triggers, then resume succeeds\n- Resume with awaitCompletion and immediate copilot exit (0 events added) \u2014 summary is null\n- Concurrent resume attempts \u2014 exactly one succeeds, other gets SESSION_ACTIVE\n\n## CRAP / Quality Gates\n- lib/resume.mjs is ALREADY in RESUME_DETERMINISTIC_LOGIC_TARGETS \u2014 CRAP < 8 after changes\n- Add targeted mutant: --resume flag omitted from copilot command (verify test catches missing resume)\n- Add targeted mutant: new pane created but old pane not verified dead (verify test catches stale pane)\n- Add targeted mutant: session lock not acquired before resume (verify test catches unlocked resume)\n- Add targeted mutant: copilotSessionId used instead of paneSessionId for tmux target (verify test catches wrong ID)\n- Run npm run test:crap and npm run test:mutation after implementation\n## Cold-Start Agent Guide\n\n### DI Pattern (copy this)\nAll new modules follow the dependency-injection pattern. See `lib/launch.mjs:22-59` for the canonical example:\n```javascript\nexport function myService(args, services = {}) {\n  const { readFileSync = fs.readFileSync, writeFileSync = fs.writeFileSync } = services;\n  // ... use injected operations\n}\n```\nTests inject stubs: `myService(args, { readFileSync: () => '...' })`\n\n### Test Harness (copy this)\nImport modules via red-harness for export validation:\n```javascript\nimport { importProjectModule } from './helpers/red-harness.mjs';\nconst { myFunction } = await importProjectModule('lib/my-module.mjs');\n```\nSee `test/resume.test.mjs:69-78` for a complete example with DI stubs.\n\n### Verification\n\u26a0\ufe0f **Blocked by TIX-000015**: `test/unit/` directory and `npm run test:unit` do not exist yet.\nUntil TIX-000015 lands, unit tests for this slice cannot be run via the standard workflow.\nIf working in parallel, run the test file directly: `node --test test/unit/<filename>.test.mjs`\n\n### Path Construction\nUse `path.join(os.homedir(), '.copilot', 'session-state', copilotSessionId, ...)` \u2014 NOT `~/.copilot/...`.\nNode path APIs do not expand `~`.\n### copilotSessionId vs sessionId\nAccept `copilotSessionId` as a direct parameter. Do NOT use the manifest's legacy `sessionId` field.\n`copilotSessionId` is the UUID from `copilot --resume=<UUID>` and maps to `~/.copilot/session-state/<UUID>/`.", "assignee": null, "tags": ["interactive-subagents-v1", "wave-4"], "agentState": "Todo", "agentStage": "Proposed", "ado": null, "parent": "TIX-000013", "blockedBy": ["TIX-000018", "TIX-000020"], "subtasks": [], "completion": null, "close": {"status": "open", "pr": null, "closedAt": null}}
{"schema": "tix/v1", "id": "TIX-000024", "type": "Deliverable", "description": "Slice 9: Launch orchestration with closePane, fork, and lifecycle guards", "context": "## Traceability\n- **Shared Key**: interactive-subagents-v1\n- **Spec Path**: specs/subagents/interactive-subagents-v1.md\n- **Spec Lines**: 209-248 (Data/Control Flow), 154-155 (extract\u2192manifest\u2192close), 708-732 (Lifecycle Guards), 724-730 (probeSessionLiveness)\n\n## Acceptance Criteria\n\nGIVEN an autonomous launch completes (sentinel detected)\nWHEN the completion handler runs\nTHEN sequence is: extract summary \u2192 update manifest to terminal \u2192 close pane\n\nGIVEN interactive: true\nWHEN copilot_subagent_launch is called\nTHEN no sentinel wrapper is used, command sent directly, status is \"interactive\", return is immediate\n\nGIVEN fork: { launchId: \"xyz\" }\nWHEN copilot_subagent_launch is called\nTHEN parent session is forked first, child launched with --resume=<forkUUID>\n\nGIVEN closePaneOnCompletion: false\nWHEN autonomous launch completes\nTHEN summary is extracted, manifest updated, but pane is NOT closed\n\nGIVEN a pane disappears before sentinel (mid-execution crash)\nWHEN the poller detects pane gone\nTHEN manifest transitions to failure, best-effort summary extraction attempted\n\nGIVEN probeSessionLiveness is called with a manifest\nWHEN the pane no longer exists\nTHEN it returns { alive: false, exitCode: null }\n\n## Must-Haves\n- **Truths**: extract\u2192manifest\u2192close sequence; interactive skips sentinel; fork integrates; closePaneOnCompletion respected; liveness probing works\n- **Artifacts**: lib/launch.mjs (updated), lib/mux.mjs (updated \u2014 probeSessionLiveness), test/single-launch.test.mjs (updated)\n- **Key Links**: Integrates closePane (D5), sessionLock (D4), forkSession (D7), extractSessionSummary (D6)\n\n## Implementation Notes\n- lib/launch.mjs is the orchestration layer \u2014 it calls all the new services\n- probeSessionLiveness goes in lib/mux.mjs (backend-specific): tmux has-pane + list-panes pane_current_command\n- Interactive launches: skip sentinel wrapper, send command directly to pane, write manifest as \"interactive\", return immediately\n- Fork launches: acquire parent lock \u2192 call forkSession \u2192 release parent lock \u2192 proceed with launch using fork UUID\n- copilotSessionId generation: crypto.randomUUID() at launch time, stored in manifest\n- Follow existing resolve* pattern for injecting new deps: resolveClosePane, resolveSessionLock, resolveForkSession\n\n## Adversarial Test Cases\n- Launch with fork from nonexistent launchId \u2014 returns LAUNCH_NOT_FOUND\n- Launch with fork from active parent \u2014 returns SESSION_ACTIVE\n- Autonomous launch where closePane throws (pane already gone) \u2014 manifest still updated correctly\n\n## CRAP / Quality Gates\n- lib/launch.mjs is ALREADY in DETERMINISTIC_LOGIC_TARGETS \u2014 CRAP < 8 after changes\n- Add targeted mutant: closePane not called on subagent failure (verify test catches leaked panes)\n- Add targeted mutant: fork called without session lock held (verify test catches unlocked fork)\n- Add targeted mutant: orchestration timeout not enforced (verify test catches runaway subagents)\n- Run npm run test:crap and npm run test:mutation after implementation\n## Cold-Start Agent Guide\n\n### DI Pattern (copy this)\nAll new modules follow the dependency-injection pattern. See `lib/launch.mjs:22-59` for the canonical example:\n```javascript\nexport function myService(args, services = {}) {\n  const { readFileSync = fs.readFileSync, writeFileSync = fs.writeFileSync } = services;\n  // ... use injected operations\n}\n```\nTests inject stubs: `myService(args, { readFileSync: () => '...' })`\n\n### Test Harness (copy this)\nImport modules via red-harness for export validation:\n```javascript\nimport { importProjectModule } from './helpers/red-harness.mjs';\nconst { myFunction } = await importProjectModule('lib/my-module.mjs');\n```\nSee `test/resume.test.mjs:69-78` for a complete example with DI stubs.\n\n### Verification\n\u26a0\ufe0f **Blocked by TIX-000015**: `test/unit/` directory and `npm run test:unit` do not exist yet.\nUntil TIX-000015 lands, unit tests for this slice cannot be run via the standard workflow.\nIf working in parallel, run the test file directly: `node --test test/unit/<filename>.test.mjs`\n", "assignee": null, "tags": ["interactive-subagents-v1", "wave-4"], "agentState": "Todo", "agentStage": "Proposed", "ado": null, "parent": "TIX-000013", "blockedBy": ["TIX-000018", "TIX-000019", "TIX-000021"], "subtasks": [], "completion": null, "close": {"status": "open", "pr": null, "closedAt": null}}
{"schema": "tix/v1", "id": "TIX-000025", "type": "Deliverable", "description": "Slice 10: Tool schemas, parallel passthrough, and backend preference", "context": "## Traceability\n- **Shared Key**: interactive-subagents-v1\n- **Spec Path**: specs/subagents/interactive-subagents-v1.md\n- **Spec Lines**: 630-654 (Launch Parameters), 48-49 (parallel touchpoint), 46 (backend preference)\n\n## Acceptance Criteria\n\nGIVEN copilot_subagent_launch tool schema\nWHEN a caller provides interactive, fork, or closePaneOnCompletion params\nTHEN the tool accepts and passes them to the launch orchestration\n\nGIVEN copilot_subagent_resume tool schema\nWHEN a caller provides an optional task param\nTHEN the tool accepts it and passes to resume flow\n\nGIVEN copilot_subagent_parallel is called with agents that include interactive, fork, or closePaneOnCompletion\nWHEN the parallel orchestrator runs\nTHEN each individual launch receives those v1 params\n\nGIVEN both tmux and zellij are available\nWHEN backend auto-detection runs\nTHEN zellij is preferred (selected first)\n\nGIVEN awaitCompletion is not specified\nWHEN interactive: true\nTHEN awaitCompletion defaults to false\n\nGIVEN awaitCompletion is not specified\nWHEN interactive: false (autonomous)\nTHEN awaitCompletion defaults to true\n\n## Must-Haves\n- **Truths**: tool schemas accept v1 params; parallel delegates (does not re-implement); zellij preferred; mode-aware defaults\n- **Artifacts**: extension.mjs (tool schemas updated), lib/parallel.mjs (updated), lib/mux.mjs (backend order updated)\n- **Key Links**: Tool schemas in extension.mjs are the public API \u2014 changes here affect all callers\n\n## Implementation Notes\n- Tool schemas are at extension.mjs ~lines 42-220. Add interactive (boolean), fork (object), closePaneOnCompletion (boolean) to launch schema\n- Add task (string, optional) to resume schema\n- parallel.mjs delegates to individual copilot_subagent_launch calls \u2014 just pass through new params, no new logic\n- Backend preference in lib/mux.mjs: reorder SUPPORTED_BACKENDS to try zellij before tmux\n- awaitCompletion default logic: check interactive flag, set default accordingly\n- cmux backend: log warning and ignore interactive/fork/closePaneOnCompletion params\n\n## Adversarial Test Cases\n- Parallel launch with mixed interactive/autonomous agents \u2014 each gets correct defaults\n- Launch with fork param but cmux backend \u2014 warning logged, fork ignored, launch proceeds as autonomous\n- Backend detection when only tmux available \u2014 tmux selected (zellij preference is not a hard requirement)\n\n## CRAP / Quality Gates\n- extension.mjs, lib/parallel.mjs, lib/mux.mjs are ALREADY in DETERMINISTIC_LOGIC_TARGETS \u2014 CRAP < 8 after changes\n- Add targeted mutant: required tool parameter marked optional in schema (verify test catches loose validation)\n- Add targeted mutant: tool handler returns raw result instead of structured envelope (verify test catches)\n- Add targeted mutant: tool name misspelled in registration (verify test catches missing tool)\n- Run npm run test:crap and npm run test:mutation after implementation\n## Cold-Start Agent Guide\n\n### DI Pattern (copy this)\nAll new modules follow the dependency-injection pattern. See `lib/launch.mjs:22-59` for the canonical example:\n```javascript\nexport function myService(args, services = {}) {\n  const { readFileSync = fs.readFileSync, writeFileSync = fs.writeFileSync } = services;\n  // ... use injected operations\n}\n```\nTests inject stubs: `myService(args, { readFileSync: () => '...' })`\n\n### Test Harness (copy this)\nImport modules via red-harness for export validation:\n```javascript\nimport { importProjectModule } from './helpers/red-harness.mjs';\nconst { myFunction } = await importProjectModule('lib/my-module.mjs');\n```\nSee `test/resume.test.mjs:69-78` for a complete example with DI stubs.\n\n### Verification\n\u26a0\ufe0f **Blocked by TIX-000015**: `test/unit/` directory and `npm run test:unit` do not exist yet.\nUntil TIX-000015 lands, unit tests for this slice cannot be run via the standard workflow.\nIf working in parallel, run the test file directly: `node --test test/unit/<filename>.test.mjs`\n", "assignee": null, "tags": ["interactive-subagents-v1", "wave-4"], "agentState": "Todo", "agentStage": "Proposed", "ado": null, "parent": "TIX-000013", "blockedBy": ["TIX-000016", "TIX-000017"], "subtasks": [], "completion": null, "close": {"status": "open", "pr": null, "closedAt": null}}
{"schema": "tix/v1", "id": "TIX-000026", "type": "Deliverable", "description": "Slice 11: E2E test suite with real Copilot CLI and tmux panes", "context": "## Traceability\n- **Shared Key**: interactive-subagents-v1\n- **Spec Path**: specs/subagents/interactive-subagents-v1.md\n- **Spec Lines**: 855-916 (E2E Tests), 918-932 (Cross-Service Scenario Matrix)\n\n## Acceptance Criteria\n\nGIVEN the extension is deployed to ~/.copilot/extensions/\nWHEN copilot is launched in a tmux pane with a test prompt\nTHEN extension tools are available (TTY-dependent loading confirmed)\n\nGIVEN an autonomous launch E2E test\nWHEN the test runs to completion\nTHEN: pane created \u2192 sentinel detected \u2192 pane closed \u2192 manifest in terminal state \u2192 session dir exists \u2192 summary extracted\n\nGIVEN an interactive launch E2E test\nWHEN the test runs\nTHEN: pane created \u2192 status \"interactive\" \u2192 pane stays alive \u2192 copilot process running\n\nGIVEN a resume E2E test\nWHEN a completed session is resumed\nTHEN: new pane opens \u2192 --resume=<UUID> in command \u2192 context restored (events.jsonl grows)\n\nGIVEN a fork E2E test\nWHEN a session is forked and launched\nTHEN: parent events.jsonl unchanged \u2192 child has new UUID \u2192 child events.jsonl grows independently\n\nGIVEN zellij is not installed\nWHEN the zellij E2E test would run\nTHEN it is skipped gracefully (not a hard failure)\n\n## Must-Haves\n- **Truths**: autonomous flow works end-to-end; interactive flow works; resume restores context; fork isolates; tests are self-cleaning\n- **Artifacts**: test/e2e/autonomous-launch.test.mjs, test/e2e/interactive-launch.test.mjs, test/e2e/resume-session.test.mjs, test/e2e/fork-launch.test.mjs\n- **Key Links**: E2E harness copies extension to ~/.copilot/extensions/ \u2014 must restore original on cleanup\n\n## Implementation Notes\n- All E2E tests run in isolated tmux sessions: tmux new-session -d -s \"e2e-test-<UUID>\"\n- Cleanup: tmux kill-session -t \"e2e-test-<UUID>\" + remove test workspace + session state\n- Copilot flags: --allow-all-tools --allow-all-paths --allow-all-urls --model <cheap-model> --no-ask-user\n- Primary verification: launch manifest JSON + session state files. NOT screen text.\n- Timeout: 60s per test. Skip if copilot CLI or tmux not available.\n- Do NOT use extensions_reload \u2014 it kills sidecars permanently. Deploy to ~/.copilot/extensions/ and start fresh pane.\n- Test prompts should be minimal: \"Call copilot_subagent_launch with task X, then report the launchId\"\n\n## Adversarial Test Cases\n- E2E where copilot crashes mid-task \u2014 manifest reaches terminal state (failure), no orphan panes\n- E2E where pane is manually killed during test \u2014 timeout fires, manifest updated, test reports failure cleanly\n- Parallel E2E: two simultaneous launches \u2014 both complete with separate manifests\n## Cold-Start Agent Guide\n\n### DI Pattern (copy this)\nAll new modules follow the dependency-injection pattern. See `lib/launch.mjs:22-59` for the canonical example:\n```javascript\nexport function myService(args, services = {}) {\n  const { readFileSync = fs.readFileSync, writeFileSync = fs.writeFileSync } = services;\n  // ... use injected operations\n}\n```\nTests inject stubs: `myService(args, { readFileSync: () => '...' })`\n\n### Test Harness (copy this)\nImport modules via red-harness for export validation:\n```javascript\nimport { importProjectModule } from './helpers/red-harness.mjs';\nconst { myFunction } = await importProjectModule('lib/my-module.mjs');\n```\nSee `test/resume.test.mjs:69-78` for a complete example with DI stubs.\n", "assignee": null, "tags": ["interactive-subagents-v1", "wave-5"], "agentState": "Todo", "agentStage": "Proposed", "ado": null, "parent": "TIX-000014", "blockedBy": ["TIX-000023", "TIX-000024", "TIX-000025"], "subtasks": [], "completion": null, "close": {"status": "open", "pr": null, "closedAt": null}}
